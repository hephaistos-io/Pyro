/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { UpdateTierRequest } from '../../models/update-tier-request';

export interface UpdateEnvironmentTier$Params {
  environmentId: string;
      body: UpdateTierRequest
}

export function updateEnvironmentTier(http: HttpClient, rootUrl: string, params: UpdateEnvironmentTier$Params, context?: HttpContext): Observable<StrictHttpResponse<void>> {
  const rb = new RequestBuilder(rootUrl, updateEnvironmentTier.PATH, 'put');
  if (params) {
    rb.path('environmentId', params.environmentId, {});
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'text', accept: '*/*', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return (r as HttpResponse<any>).clone({ body: undefined }) as StrictHttpResponse<void>;
    })
  );
}

updateEnvironmentTier.PATH = '/v1/payment/environments/{environmentId}/tier';
