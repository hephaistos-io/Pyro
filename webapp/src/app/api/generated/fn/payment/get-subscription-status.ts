/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { SubscriptionStatusResponse } from '../../models/subscription-status-response';

export interface GetSubscriptionStatus$Params {
}

export function getSubscriptionStatus(http: HttpClient, rootUrl: string, params?: GetSubscriptionStatus$Params, context?: HttpContext): Observable<StrictHttpResponse<SubscriptionStatusResponse>> {
  const rb = new RequestBuilder(rootUrl, getSubscriptionStatus.PATH, 'get');
  if (params) {
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<SubscriptionStatusResponse>;
    })
  );
}

getSubscriptionStatus.PATH = '/v1/payment/subscription';
