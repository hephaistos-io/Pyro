/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import {HttpClient, HttpContext, HttpResponse} from '@angular/common/http';
import {Observable} from 'rxjs';
import {filter, map} from 'rxjs/operators';
import {StrictHttpResponse} from '../../strict-http-response';
import {RequestBuilder} from '../../request-builder';

import {InviteValidationResponse} from '../../models/invite-validation-response';

export interface ValidateInvite$Params {
  token: string;
}

export function validateInvite(http: HttpClient, rootUrl: string, params: ValidateInvite$Params, context?: HttpContext): Observable<StrictHttpResponse<InviteValidationResponse>> {
  const rb = new RequestBuilder(rootUrl, validateInvite.PATH, 'get');
  if (params) {
    rb.path('token', params.token, {});
  }

  return http.request(
    rb.build({responseType: 'json', accept: 'application/json', context})
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<InviteValidationResponse>;
    })
  );
}

validateInvite.PATH = '/v1/invite/{token}';
