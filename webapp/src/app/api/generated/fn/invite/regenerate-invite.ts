/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { InviteCreationResponse } from '../../models/invite-creation-response';

export interface RegenerateInvite$Params {
  id: string;
}

export function regenerateInvite(http: HttpClient, rootUrl: string, params: RegenerateInvite$Params, context?: HttpContext): Observable<StrictHttpResponse<InviteCreationResponse>> {
  const rb = new RequestBuilder(rootUrl, regenerateInvite.PATH, 'post');
  if (params) {
    rb.path('id', params.id, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<InviteCreationResponse>;
    })
  );
}

regenerateInvite.PATH = '/v1/company/invite/{id}/regenerate';
