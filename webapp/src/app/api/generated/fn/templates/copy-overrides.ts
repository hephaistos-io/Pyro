/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import {HttpClient, HttpContext, HttpResponse} from '@angular/common/http';
import {Observable} from 'rxjs';
import {filter, map} from 'rxjs/operators';
import {StrictHttpResponse} from '../../strict-http-response';
import {RequestBuilder} from '../../request-builder';

import {CopyOverridesRequest} from '../../models/copy-overrides-request';
import {CopyOverridesResponse} from '../../models/copy-overrides-response';

export interface CopyOverrides$Params {
  applicationId: string;
  body: CopyOverridesRequest
}

export function copyOverrides(http: HttpClient, rootUrl: string, params: CopyOverrides$Params, context?: HttpContext): Observable<StrictHttpResponse<CopyOverridesResponse>> {
  const rb = new RequestBuilder(rootUrl, copyOverrides.PATH, 'post');
  if (params) {
    rb.path('applicationId', params.applicationId, {});
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({responseType: 'json', accept: 'application/json', context})
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<CopyOverridesResponse>;
    })
  );
}

copyOverrides.PATH = '/v1/applications/{applicationId}/templates/copy-overrides';
