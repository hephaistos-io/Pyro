/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { TemplateType } from '../../models/template-type';

export interface DeleteOverride$Params {
  applicationId: string;
  type: TemplateType;
  environmentId: string;
  identifier: string;
}

export function deleteOverride(http: HttpClient, rootUrl: string, params: DeleteOverride$Params, context?: HttpContext): Observable<StrictHttpResponse<void>> {
  const rb = new RequestBuilder(rootUrl, deleteOverride.PATH, 'delete');
  if (params) {
    rb.path('applicationId', params.applicationId, {});
    rb.path('type', params.type, {});
    rb.path('environmentId', params.environmentId, {});
    rb.path('identifier', params.identifier, {});
  }

  return http.request(
    rb.build({ responseType: 'text', accept: '*/*', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return (r as HttpResponse<any>).clone({ body: undefined }) as StrictHttpResponse<void>;
    })
  );
}

deleteOverride.PATH = '/v1/applications/{applicationId}/templates/{type}/environments/{environmentId}/overrides/{identifier}';
