/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { TemplateResponse } from '../../models/template-response';
import { TemplateType } from '../../models/template-type';

export interface GetTemplates$Params {
  applicationId: string;

/**
 * Filter by template type (USER or SYSTEM). If not provided, returns all templates.
 */
  type?: TemplateType;
}

export function getTemplates(http: HttpClient, rootUrl: string, params: GetTemplates$Params, context?: HttpContext): Observable<StrictHttpResponse<Array<TemplateResponse>>> {
  const rb = new RequestBuilder(rootUrl, getTemplates.PATH, 'get');
  if (params) {
    rb.path('applicationId', params.applicationId, {});
    rb.query('type', params.type, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<Array<TemplateResponse>>;
    })
  );
}

getTemplates.PATH = '/v1/applications/{applicationId}/templates';
