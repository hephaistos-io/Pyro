/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { TemplateType } from '../../models/template-type';
import { TemplateValuesRequest } from '../../models/template-values-request';
import { TemplateValuesResponse } from '../../models/template-values-response';

export interface SetOverride$Params {
  applicationId: string;
  type: TemplateType;
  environmentId: string;
  identifier: string;
      body: TemplateValuesRequest
}

export function setOverride(http: HttpClient, rootUrl: string, params: SetOverride$Params, context?: HttpContext): Observable<StrictHttpResponse<TemplateValuesResponse>> {
  const rb = new RequestBuilder(rootUrl, setOverride.PATH, 'put');
  if (params) {
    rb.path('applicationId', params.applicationId, {});
    rb.path('type', params.type, {});
    rb.path('environmentId', params.environmentId, {});
    rb.path('identifier', params.identifier, {});
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<TemplateValuesResponse>;
    })
  );
}

setOverride.PATH = '/v1/applications/{applicationId}/templates/{type}/environments/{environmentId}/overrides/{identifier}';
