/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { TemplateType } from '../../models/template-type';
import { TemplateValuesResponse } from '../../models/template-values-response';

export interface ListOverrides$Params {
  applicationId: string;
  type: TemplateType;
  environmentId: string;
}

export function listOverrides(http: HttpClient, rootUrl: string, params: ListOverrides$Params, context?: HttpContext): Observable<StrictHttpResponse<Array<TemplateValuesResponse>>> {
  const rb = new RequestBuilder(rootUrl, listOverrides.PATH, 'get');
  if (params) {
    rb.path('applicationId', params.applicationId, {});
    rb.path('type', params.type, {});
    rb.path('environmentId', params.environmentId, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<Array<TemplateValuesResponse>>;
    })
  );
}

listOverrides.PATH = '/v1/applications/{applicationId}/templates/{type}/environments/{environmentId}/overrides';
