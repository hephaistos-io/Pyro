/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { MergedTemplateValuesResponse } from '../../models/merged-template-values-response';
import { TemplateType } from '../../models/template-type';

export interface GetMergedValues$Params {
  applicationId: string;
  type: TemplateType;
  environmentId: string;

/**
 * Identifiers to merge, in order of precedence
 */
  identifiers?: Array<string>;
}

export function getMergedValues(http: HttpClient, rootUrl: string, params: GetMergedValues$Params, context?: HttpContext): Observable<StrictHttpResponse<MergedTemplateValuesResponse>> {
  const rb = new RequestBuilder(rootUrl, getMergedValues.PATH, 'get');
  if (params) {
    rb.path('applicationId', params.applicationId, {});
    rb.path('type', params.type, {});
    rb.path('environmentId', params.environmentId, {});
    rb.query('identifiers', params.identifiers, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<MergedTemplateValuesResponse>;
    })
  );
}

getMergedValues.PATH = '/v1/applications/{applicationId}/templates/{type}/environments/{environmentId}/values';
