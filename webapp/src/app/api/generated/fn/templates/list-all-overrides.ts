/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import {HttpClient, HttpContext, HttpResponse} from '@angular/common/http';
import {Observable} from 'rxjs';
import {filter, map} from 'rxjs/operators';
import {StrictHttpResponse} from '../../strict-http-response';
import {RequestBuilder} from '../../request-builder';

import {AllTemplateOverridesResponse} from '../../models/all-template-overrides-response';

export interface ListAllOverrides$Params {
  applicationId: string;
  environmentId: string;
}

export function listAllOverrides(http: HttpClient, rootUrl: string, params: ListAllOverrides$Params, context?: HttpContext): Observable<StrictHttpResponse<AllTemplateOverridesResponse>> {
  const rb = new RequestBuilder(rootUrl, listAllOverrides.PATH, 'get');
  if (params) {
    rb.path('applicationId', params.applicationId, {});
    rb.query('environmentId', params.environmentId, {});
  }

  return http.request(
    rb.build({responseType: 'json', accept: 'application/json', context})
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<AllTemplateOverridesResponse>;
    })
  );
}

listAllOverrides.PATH = '/v1/applications/{applicationId}/templates/overrides';
