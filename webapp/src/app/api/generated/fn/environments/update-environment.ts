/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { EnvironmentResponse } from '../../models/environment-response';
import { EnvironmentUpdateRequest } from '../../models/environment-update-request';

export interface UpdateEnvironment$Params {
  applicationId: string;
  environmentId: string;
      body: EnvironmentUpdateRequest
}

export function updateEnvironment(http: HttpClient, rootUrl: string, params: UpdateEnvironment$Params, context?: HttpContext): Observable<StrictHttpResponse<EnvironmentResponse>> {
  const rb = new RequestBuilder(rootUrl, updateEnvironment.PATH, 'put');
  if (params) {
    rb.path('applicationId', params.applicationId, {});
    rb.path('environmentId', params.environmentId, {});
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<EnvironmentResponse>;
    })
  );
}

updateEnvironment.PATH = '/v1/applications/{applicationId}/environments/{environmentId}';
