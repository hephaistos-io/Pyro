/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import {HttpClient, HttpContext, HttpResponse} from '@angular/common/http';
import {Observable} from 'rxjs';
import {filter, map} from 'rxjs/operators';
import {StrictHttpResponse} from '../../strict-http-response';
import {RequestBuilder} from '../../request-builder';

import {EnvironmentResponse} from '../../models/environment-response';

export interface GetEnvironments$Params {
  applicationId: string;
}

export function getEnvironments(http: HttpClient, rootUrl: string, params: GetEnvironments$Params, context?: HttpContext): Observable<StrictHttpResponse<Array<EnvironmentResponse>>> {
  const rb = new RequestBuilder(rootUrl, getEnvironments.PATH, 'get');
  if (params) {
    rb.path('applicationId', params.applicationId, {});
  }

  return http.request(
    rb.build({responseType: 'json', accept: 'application/json', context})
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<Array<EnvironmentResponse>>;
    })
  );
}

getEnvironments.PATH = '/v1/applications/{applicationId}/environments';
