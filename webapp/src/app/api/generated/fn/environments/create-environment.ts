/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import {HttpClient, HttpContext, HttpResponse} from '@angular/common/http';
import {Observable} from 'rxjs';
import {filter, map} from 'rxjs/operators';
import {StrictHttpResponse} from '../../strict-http-response';
import {RequestBuilder} from '../../request-builder';

import {EnvironmentCreationRequest} from '../../models/environment-creation-request';
import {EnvironmentResponse} from '../../models/environment-response';

export interface CreateEnvironment$Params {
  applicationId: string;
  body: EnvironmentCreationRequest
}

export function createEnvironment(http: HttpClient, rootUrl: string, params: CreateEnvironment$Params, context?: HttpContext): Observable<StrictHttpResponse<EnvironmentResponse>> {
  const rb = new RequestBuilder(rootUrl, createEnvironment.PATH, 'post');
  if (params) {
    rb.path('applicationId', params.applicationId, {});
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({responseType: 'json', accept: 'application/json', context})
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<EnvironmentResponse>;
    })
  );
}

createEnvironment.PATH = '/v1/applications/{applicationId}/environments';
