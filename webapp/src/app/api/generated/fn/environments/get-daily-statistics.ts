/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { DailyUsageStatisticsResponse } from '../../models/daily-usage-statistics-response';

export interface GetDailyStatistics$Params {
  applicationId: string;
  environmentId: string;
  days?: number;
}

export function getDailyStatistics(http: HttpClient, rootUrl: string, params: GetDailyStatistics$Params, context?: HttpContext): Observable<StrictHttpResponse<Array<DailyUsageStatisticsResponse>>> {
  const rb = new RequestBuilder(rootUrl, getDailyStatistics.PATH, 'get');
  if (params) {
    rb.path('applicationId', params.applicationId, {});
    rb.path('environmentId', params.environmentId, {});
    rb.query('days', params.days, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<Array<DailyUsageStatisticsResponse>>;
    })
  );
}

getDailyStatistics.PATH = '/v1/applications/{applicationId}/environments/{environmentId}/statistics/daily';
