/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { CompanyStatisticsResponse } from '../../models/company-statistics-response';

export interface GetCompanyStatistics$Params {
}

export function getCompanyStatistics(http: HttpClient, rootUrl: string, params?: GetCompanyStatistics$Params, context?: HttpContext): Observable<StrictHttpResponse<CompanyStatisticsResponse>> {
  const rb = new RequestBuilder(rootUrl, getCompanyStatistics.PATH, 'get');
  if (params) {
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<CompanyStatisticsResponse>;
    })
  );
}

getCompanyStatistics.PATH = '/v1/company/statistics';
