# FlagForge Sandbox Environment Variables
# This file is used for Stripe sandbox (real test API) testing
#
# SETUP:
# 1. Get your test API keys from https://dashboard.stripe.com/test/apikeys
# 2. Create products/prices in Stripe dashboard (Test Mode)
# 3. Run: stripe listen --forward-to http://localhost:8080/api/webhooks/stripe
# 4. Copy the webhook signing secret from the CLI output
# 5. Fill in the values below

# =============================================================================
# Database Configuration (same as local.env)
# =============================================================================
POSTGRES_HOST=postgres
POSTGRES_PORT=5432
POSTGRES_DB=flagforge
POSTGRES_USER=flagforge
POSTGRES_PASSWORD=flagforge

# Webapp API database credentials
WEBAPP_DB_USERNAME=webapp-flagforge
WEBAPP_DB_PASSWORD=webapp-flagforge

# Customer API database credentials (read-only)
CUSTOMER_DB_USERNAME=customer-flagforge
CUSTOMER_DB_PASSWORD=customer-flagforge

# =============================================================================
# Redis Configuration
# =============================================================================
REDIS_HOST=redis
REDIS_PORT=6379
REDIS_URI=redis://redis:6379
FLAGFORGE_REDIS_ENABLED=true

# =============================================================================
# Security Configuration
# =============================================================================
FLAGFORGE_JWT_SECRET=4407a837731d92425e627e49632afbabd538a25e080fafed65a9ff7e71a9f5d1
FLAGFORGE_CORS_ALLOWED_ORIGINS=http://localhost,http://localhost:4200

# =============================================================================
# Spring Configuration
# =============================================================================
SPRING_PROFILES_ACTIVE=docker

# =============================================================================
# Stripe Sandbox Configuration (FILL IN YOUR TEST VALUES)
# =============================================================================
# Mode: sandbox uses real Stripe test API
STRIPE_MODE=sandbox

# Get from: https://dashboard.stripe.com/test/apikeys
STRIPE_API_KEY=*
STRIPE_PUBLISHABLE_KEY=*

# Get from: stripe listen --print-secret
STRIPE_WEBHOOK_SECRET=*

# =============================================================================
# Stripe Price IDs (create in Stripe Dashboard Test Mode)
# =============================================================================
# Go to Products > Add Product > Create pricing
# Copy the price_xxx IDs here
STRIPE_PRICE_BASIC=*
STRIPE_PRICE_STANDARD=*
STRIPE_PRICE_PRO=*
STRIPE_PRICE_BUSINESS=*
