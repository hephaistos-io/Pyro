spring:
  main:
    allow-bean-definition-overriding: true

  flyway:
    enabled: true
    # Migrations from webapp-api via sourceSets configuration
    locations: classpath:db/migration

  jpa:
    hibernate:
      ddl-auto: validate

logging:
  level:
    org.flywaydb: DEBUG
